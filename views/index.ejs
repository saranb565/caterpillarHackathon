<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Professional Website</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      overflow: hidden;
      font-family: 'Arial', sans-serif;
      position: relative;
      color: #060000;
    }
    
    .cover-container {
      max-width: 42em;
      padding: 3rem 1.5rem;
      text-align: center;
      margin: 15%;
      margin-left: 25%;
      padding-bottom: 40vh;
      position: relative;
      z-index: 1;
      opacity: 1; /* Lower the opacity of the content */
      display: flex;
      flex-direction: column;
      justify-content: center;
      min-height: 100vh; /* Ensure the container takes full viewport height */
    }

    .mb-5 {
      align-items: center;
      justify-content: center;
      align-self: center;
      justify-items: center;
      justify-self: center;
    }

    .cover-heading {
      font-size: 3.5rem;
      font-weight: bold;
    }

    .lead {
      font-size: 1.25rem;
      font-weight: 300;
    }

    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
    }

    .btn-outline-light {
      color: #000000; /* Text color */
      border-color: #000000; /* Border color */
    }

    .btn-outline-light:hover {
      background-color: #000000; /* Background color on hover */
      color: #ffffff; /* Text color on hover */
    }

    img {
      max-width: 100%;
      height: auto;
      width: 300px; /* Set the width to an appropriate size */
      border-radius: 10px; /* Optional: Add rounded corners to the image */
    }

    /* Background blur effect */
    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('images/ss.png'); /* Background image */
      background-size: cover;
      background-position: center;
      filter: blur(4px); /* Apply Gaussian blur */
      opacity: 0.5; /* Lower the opacity of the blurred background */
      z-index: -1; /* Place behind the content */
    }

    header, main, footer {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
  </style>
</head>
<body>
  <div class="cover-container">
    <header class="mb-5">
      <img src="<%= '/images/cat1.jpg' %>" alt="idk">
      <h2 class="lead mt-3">Let's do the WORK</h2>
    </header>

    <main>
      <p class="lead">
        <a href="/inspection" class="btn btn-outline-light btn-lg mx-2">Start Inspecting</a>
      </p>
    </main>

    <footer class="mt-5">
      <p>© 2024 My Name. All rights reserved.</p>
    </footer>
  </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>