<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .navbar-custom {
            background-color: yellow;
            width: 100%;
            position: fixed; /* Make header fixed */
            top: 0; /* Align at the top of the viewport */
            left: 0; /* Align at the left edge of the viewport */
            z-index: 1000; /* Ensure header stays on top of other content */
        }
        .navbar-custom .nav-link {
            color: black;
        }
        .navbar-custom .nav-link.active {
            background-color: black;
            color: yellow;
        }
    </style>
</head>
<body>
    <!--HEADER-->
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom navbar-custom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
            <span class="fs-4">Simple header</span>
        </a>

        <ul class="nav nav-pills">
            <li class="nav-item"><a href="#" class="nav-link active" aria-current="page">Home</a></li>
            <li class="nav-item"><a href="#" class="nav-link">Features</a></li>
            <li class="nav-item"><a href="#" class="nav-link">Pricing</a></li>
            <li class="nav-item"><a href="#" class="nav-link">FAQs</a></li>
            <li class="nav-item"><a href="#" class="nav-link">About</a></li>
        </ul>
    </header>

    <div class="container mt-5 pt-5">
        <!--CONTENT-->
        <h1>Battery Inspection Form</h1>
        <form action="/<%=next.toLowerCase()%>">
            <!-- Battery Make -->
            <div class="mb-3">
                <label for="batteryMake" class="form-label">Battery Make</label>
                <input type="text" class="form-control" id="batteryMake" placeholder="Example: CAT, ABC, XYZ, etc." required>
            </div>

            <!-- Battery Replacement Date -->
            <div class="mb-3">
                <label for="batteryReplacementDate" class="form-label">Battery Replacement Date</label>
                <input type="date" class="form-control" id="batteryReplacementDate" required>
            </div>

            <!-- Battery Voltage -->
            <div class="mb-3">
                <label for="batteryVoltage" class="form-label">Battery Voltage</label>
                <input type="text" class="form-control" id="batteryVoltage" placeholder="Example: 12V, 13V" required>
            </div>

            <!-- Battery Water Level -->
            <div class="mb-3">
                <label for="batteryWaterLevel" class="form-label">Battery Water Level</label>
                <select class="form-select" id="batteryWaterLevel" required>
                    <option value="">Select water level</option>
                    <option value="Good">Good</option>
                    <option value="Ok">Ok</option>
                    <option value="Low">Low</option>
                </select>
            </div>

            <!-- Condition of Battery -->
            <div class="mb-3">
                <label for="batteryCondition" class="form-label">Condition of Battery (Any damage)</label>
                <select class="form-select" id="batteryCondition" required>
                    <option value="">Select condition</option>
                    <option value="Yes">Yes</option>
                    <option value="No">No</option>
                </select>
            </div>

            <!-- Battery Damage Image (if any) -->
            <div class="mb-3">
                <label for="batteryDamageImage" class="form-label">Attach image of damage (if any)</label>
                <input type="file" class="form-control" id="batteryDamageImage" accept="image/*">
            </div>

            <!-- Any Leak / Rust in Battery -->
            <div class="mb-3">
                <label for="batteryLeakRust" class="form-label">Any Leak / Rust in Battery</label>
                <select class="form-select" id="batteryLeakRust" required>
                    <option value="">Select option</option>
                    <option value="Yes">Yes</option>
                    <option value="No">No</option>
                </select>
            </div>

            <!-- Battery Overall Summary -->
            <div class="mb-3">
                <label for="batterySummary" class="form-label">Battery Overall Summary</label>
                <textarea class="form-control" id="batterySummary" rows="3" maxlength="1000" placeholder="Provide an overall summary of the battery condition (max 1000 characters)" required></textarea>
            </div>

            <!-- Battery Images -->
            <div class="mb-3">
                <label for="batteryImages" class="form-label">Attach images of the battery</label>
                <input type="file" class="form-control" id="batteryImages" multiple accept="image/*" required>
            </div>

            <button type="submit" class="btn btn-primary">Submit and proceed to <%=next%></button>
        </form>
    </div>

    <!--FOOTER-->
    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <div class="col-md-4 d-flex align-items-center">
            <a href="/" class="mb-3 me-2 mb-md-0 text-body-secondary text-decoration-none lh-1">
                <svg class="bi" width="30" height="24"><use xlink:href="#bootstrap"></use></svg>
            </a>
            <span class="mb-3 mb-md-0 text-body-secondary">© 2024 Company, Inc</span>
        </div>

        <ul class="nav col-md-4 justify-content-end list-unstyled d-flex">
            <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#twitter"></use></svg></a></li>
            <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#instagram"></use></svg></a></li>
            <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#facebook"></use></svg></a></li>
        </ul>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
